version: '3.8'

services:
  tiktok-ruleta:
    build: .
    network_mode: "host"
    volumes:
      - ruleta-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3005
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
    container_name: tiktok-ruleta-app

volumes:
  ruleta-data:
    driver: local
